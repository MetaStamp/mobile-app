import { normalize } from "../utils/size";

export const sizes = {
  margin: {
    small: normalize(10),
  },
  padding: {
    tiny: normalize(5),
    small: normalize(10),
  },
  fonts: {
    h1: normalize(23),
  },
  borders: {
    small: normalize(1)
  },
  images: {
    logo: normalize(100)
  }
}
